#include <bits/stdc++.h>
#define REP(i,n) for(int i = 0; i< (n);i++)
#define all(x) (x).begin(), (x).end()
#define f first  
#define s second
#define SZ(a) (int)a.size()
#define bit(x,i)(((x)>>(i))&1)
typedef long long ll;
using namespace std; 

int guess(string ops, int num){
	fflush(stdout);
	cout << ops << " " << num << "\n";
	fflush(stdout);
	int ans; cin >> ans;
	fflush(stdout);
	return ans;
}

void solve(){
	
}


int main()
{
	ios::sync_with_stdio(false); cin.tie(0);
	int tt;
	cin >> tt;
	while(tt--){
		solve();
	}
}


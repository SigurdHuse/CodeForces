#include <bits/stdc++.h> 
using ll = long long;
#define REP(i,n) for(int i = 0; i< (n);i++) 
using namespace std; 

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	int tt,x,y,inc;
	cin >> tt;
	pair<int,int> one,two,three;
	while(tt--){
		cin >> x >> y;
		one = {x,y};
		cin >> x >> y;
		two = {x,y};
		cin >> x >> y;
		three = {x,y};
		long double ans = 0;
		inc = abs(one.second - two.second);
	}
}

#include <bits/stdc++.h>
#define REP(i,n) for(int i = 0; i< (n);i++)
#define all(x) (x).begin(), (x).end()
#define f first  
#define s second
#define SZ(a) (int)a.size()
#define bit(x,i)(((x)>>(i))&1)
typedef long long ll;
using namespace std; 

void solve(){
	int n, m; cin >> n >> m;
	string s; cin >> s;
	s += "a";
	set<pair<int,int>> seg;
	
	int start = 0;
	for(; start < n && s[start] != '.'; ++start) 
	
	for(int i = 0; i < )
	
	while(m--){
		int x; cin >> x; x--;
		char c; cin >> c;
	}
}

int main()
{
	ios::sync_with_stdio(false); cin.tie(0);
	solve();
}


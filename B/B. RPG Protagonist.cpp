#include <bits/stdc++.h>
#include <bits/stdc++.h>
#define REP(i,n) for(int i = 0; i< (n);i++)
#define all(x) (x).begin(), (x).end()
#define f first  
#define s second
#define SZ(a) (int)a.size()
#define bit(x,i)(((x)>>(i))&1)
typedef long long ll;
using namespace std; 

void solve(){
	int p, f; cin >> p >> f;
	if(f > p) swap(f, p); 
	
	int sword, axe; cin >> sword >> axe;
	int cs, ca; cin >> cs >> ca;
	
	int one = 0, two = 0;
	
	while((p > 0) && (f > 0) && ((sword > 0) || (axe > 0)){
		if(sword == 0){
			
		}
		
		
	}
	
	
}


int main()
{
	ios::sync_with_stdio(false); cin.tie(0);
	int tt;
	cin >> tt;
	while(tt--){
		solve();
	}
}

